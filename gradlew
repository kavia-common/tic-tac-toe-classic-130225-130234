#!/usr/bin/env bash
# Container workspace stub gradlew to satisfy CI calling ./gradlew from this directory.
# For real native builds, generate the proper wrapper via Expo prebuild:
#   cd tic_tac_toe_frontend && npm run build:android
echo "Container workspace stub gradlew: CI-safe no-op (no native project generated)."
exit 0
