#!/usr/bin/env bash
# Stub gradlew to satisfy CI environments that invoke Gradle before Expo prebuild.
# This placeholder exits successfully to avoid false negatives in CI mobile checks.
# For real native builds, run: npm run build:android (which will generate the proper Gradle wrapper).
echo "Stub gradlew: Expo prebuild not executed. This is a CI-safe no-op."
exit 0
